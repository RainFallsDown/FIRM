# Archive

This folder stores release artifacts and historical files that should not be the main entry point.

```text
firm-sft-grpo.rar        Original GRPO/SFT archive backup
```

Prefer using the organized source in `../grpo_sft/`.
